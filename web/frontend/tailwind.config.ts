// Tailwind CSS v4 configuration
// Note: Most configuration is done through CSS variables in globals.css
export default {
  // v4 uses CSS-first configuration
  // See app/globals.css for theme customization
}
